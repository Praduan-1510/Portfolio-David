/*
 * Twitter/X card. Identical composition to the root Open Graph image — re-export
 * the same renderer and its metadata so there's a single source for both cards.
 */
export { default, alt, size, contentType } from "./opengraph-image";
